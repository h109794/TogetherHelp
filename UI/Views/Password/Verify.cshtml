@model SRV.ViewModel.EmailModel
@{
    ViewBag.Title = "找回密码 - 一起帮";
    Layout = "~/Views/Shared/Layout/_UserRightLayout.cshtml";
}

@section Header{
    <i class="fa fa-unlock" aria-hidden="true"></i> 找回密码
}

@using (Html.BeginForm())
{
    <div class="form-group">
        <label><b>账号邮箱：</b></label>
        @Html.TextBoxFor(m => m.EmailAddress, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.EmailAddress)
    </div>

    <div class="form-group">
        <label><b>验证码：</b></label>
        <div class="input-group">
            @Html.TextBoxFor(m => m.VerificationCode, new { @class = "form-control" })
            <div class="input-group-append">
                <button id="send" type="button" class="btn btn-outline-secondary" disabled>
                    <span id="send-btn-text" style="font-size:14px">发送</span>
                    <i class="fa fa-envelope-o" aria-hidden="true"></i>
                </button>
            </div>
        </div>
        @Html.ValidationMessageFor(m => m.VerificationCode)
    </div>
    <button id="submit" type="submit" class="btn btn-outline-primary btn-block" disabled>提交</button>
}

@section Description{
    <ul>
        <li class="mb-2">验证码将会发送至账号绑定邮箱。</li>
        <li>如果长时间未收到验证邮件，请检查邮箱地址是否正确并尝试再次发送。</li>
    </ul>
}

<script src="~/Scripts/shared/email.js" type="text/javascript"></script>
